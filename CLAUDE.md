# CLAUDE.md — repo `alejandrehl/alejandrehl`

This repo is **Alejandro's GitHub profile README** (`github.com/Alejandrehl`).
GitHub renders `README.md` on the profile landing page. It's branding, not a product: the only
"build" is the snake workflow. Edit with rigor, commit straight to `main`, push → it's live.

## What it is (and what it is NOT)

- **Artifacts:** `README.md` (the profile card) · `assets/claude-code.svg` (hand-built Claude Code
  terminal) · `.github/workflows/snake.yml` (generates the snake).
- **VERIFIED GOTCHA (12-07-2026):** GitHub **freezes animated SVGs at their frame 0** inside the
  README (SMIL/CSS does not run in the camo-proxied embed). That's why `claude-code.svg` is designed
  with a **base state = the full, legible terminal** (all text at opacity 1, no clip that hides it);
  the animation (caret blink, sweep) is only a bonus that breaks nothing if it freezes. **Never**
  go back to a design whose frame 0 is blank. For the same reason the animated typing-SVG tagline
  was **removed** (it rendered blank) → it is now static markdown text.
- **Language:** English (GitHub's global audience) — including this file.
- It is **not** the source of truth for any domain. Business, health and personal data live in the
  Obsidian vault; multi-domain orchestration lives in `kainext-hq`.

## Live README elements (don't break)

- **Repo counter:** dynamic shields.io badge that reads `public_repos` from the GitHub API.
  **Never** hardcode a number — it updates itself.
- **Contribution snake:** generated by `.github/workflows/snake.yml` (Platane/snk, coral palette
  matching the terminal; runs on every push + every 12h) and published to the `output` branch
  (`github-contribution-grid-snake{,-dark}.svg`), embedded via `<picture>` dark/light.
- **Railway:** the operator's referral code **`KJa0VN`** → `https://railway.com?referralCode=KJa0VN`.
  Must stay in the README (badge + text line).
- **Stats cards** (github-readme-stats / streak) were dropped on purpose — they 503 frequently and
  freeze — do not re-add. Metrics go through shields badges (which do work).

## Identity (keep in sync with reality)

- **Alejandro Exequiel Hernández Lara** — Santiago, Chile. `@alejandrehl` across all platforms.
- **Ale Hernández SpA** — his **primary** company (100% his, incorporated 08-07-2026). Brand site:
  alehernandez.cl. He builds **NutriCoach** (nutricoach.cl) under it. Legal data (tax ID, deed)
  lives in the vault, **not** in this public repo.
- **KaiNext Solutions Limitada** — software consultancy (secondary, with his father Julio). kainext.cl.
- **Thoughtworks** — Senior Consultant (P4), embedded in Platform Engineering @ LATAM Airlines.

> NutriCoach is in pre-launch refinement (dogfooding), **not** publicly launched. Do not describe it
> as "launched" / "live for users" in the README.

## Cross-cutting rules (inherited from the command center)

- **plan → review → approval → execution.** Never ship substantive changes without approval.
- **10/10:** no placeholders, no unverified claims, no stale data.
- **Dates** `DD-MM-YYYY`. Verify the real time (`TZ='America/Santiago' date`) before writing any timestamp.
- **Privacy:** this repo is **public**. Never put sensitive data, secrets, third-party tax IDs, or
  internal client/engagement detail here. Public branding only.

## Workflow

Edit `README.md` → check the render (badges, links) → `git commit` → `git push origin main`.
The profile updates instantly. Badges use shields.io (inline, no dependencies).
